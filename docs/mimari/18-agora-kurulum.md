# Agora kurulumu — ARŞİV

**Agora kaldırıldı (2026-09-12).** RTC → LiveKit.

Güncel doküman: [18-livekit-kurulum.md](18-livekit-kurulum.md)

Eski `react-native-agora` / `agora-token` edge function kullanılmıyor; binary’ye dahil edilmez.
